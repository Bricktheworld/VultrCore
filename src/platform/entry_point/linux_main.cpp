#include "../../core/memory/vultr_memory.h"

namespace Vultr
{
    GameMemory *game_mem_alloc(u64 perm_storage_size, u64 temp_storage_size)
    {
        // auto *game_mem = static_cast<GameMemory *>(malloc(sizeof(GameMemory)));

        // game_mem->permanent_storage_size = perm_storage_size;

        // return game_mem;
    }

    void game_mem_free(GameMemory *game_mem)
    {
    }
} // namespace Vultr

int main(void)
{
    printf("Nukes codebase, refuses to elaborate\n");
}
